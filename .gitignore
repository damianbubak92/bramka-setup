# Local-only config copies (keep template, ignore filled-out versions if any)
config.local.sh

# Editor files
*.swp
*~
.vscode/
.idea/

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Backups
*.bak
*.orig

# TLS material - SECRETS, never commit (live under /etc/bramka/tls on the device)
*.pem
*.key
*.crt

# Server-side secrets (DB creds + shared keys). The PHP scripts require secrets.php;
# copy secrets.example.php -> secrets.php on the server and fill it in. Never commit.
secrets.php

# --- monorepo: build artifacts across toolchains ---
# CCS / TI firmware
**/Debug/
**/Release/
*.out
*.obj
*.map
# KiCad
*-backups/
fp-info-cache
*.kicad_prl
*-bak
*.lck
~*.lck
_autosave-*
# Android
**/build/
**/.gradle/
local.properties

# CCS-generated (M4F)
*.rprc
.clangd
*.rov.xs

# CC1310 build/IDE generated
**/Release/
.launches/
overview.rov.json
